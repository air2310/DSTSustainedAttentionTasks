function [trialvars] = setupTrialvars()
%setupTrialvars: Setup the variables that are used in the trial loop
%
%   Inputs:
%       N/A
%   Outputs:
%       trialvars - Variables used in the trial loop. 


trialvars.col = NaN;
trialvars.coord = NaN;
trialvars.pauser = false;
trialvars.escaper = false;

end